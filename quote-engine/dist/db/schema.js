// Five Star Conveyancing — Kysely database schema types
// Column names are snake_case to match schema.sql exactly. The repository
// layer (repository.ts) maps these onto the camelCase domain types in
// ../types.ts — nothing outside this db/ folder should import these
// row types directly.
export {};
