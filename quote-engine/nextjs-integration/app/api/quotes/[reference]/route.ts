// Illustrative wiring only — see ../route.ts for the same note.
//
// import { getQuoteHandler } from 'five-star-conveyancing-quote-engine/api/getQuote';
// import { db } from '@/lib/db';
//
// export async function GET(request: Request, { params }: { params: { reference: string } }) {
//   return getQuoteHandler(params.reference, db);
// }
